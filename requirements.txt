# Squad Fit - Python Dependencies
# ==========================================
# PINNED versions for Android compatibility
# These are tested together — do not change individually

# Core Framework
kivy==2.2.0
kivymd==1.1.1
Cython==0.29.33

# Note: These are the versions used by buildozer for Android builds
# Do NOT use kivy 2.3.x — it has no pre-built Android wheels
# Do NOT use Cython 3.x — it's incompatible with buildozer recipes
